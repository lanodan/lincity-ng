#pragma once

#define HAVE_DIRENT_H

const auto DEFAULT_LIBDIR = "@APPDATADIR@";
const auto PACKAGE_NAME = "@PACKAGE_NAME@";
const auto PACKAGE_VERSION = "@VERSION@";

constexpr auto __os_linux = 0x1;
constexpr auto __os_win = 0x2;
constexpr auto __os_mac = 0x4;

#ifdef __linux
const auto platform = __os_linux;
#elif __WIN32
const auto platform = __os_win;
#elif __APPLE__
const auto platform = __os_mac;
#endif
